@model IEnumerable<Page>

@{
    ViewData["Title"] = "Pages Index";
}

<section class="container-fluid">
    <h1 >@ViewData["Title"]</h1>

    <a asp-controller="Page" asp-action="Create" class="btn btn-outline-success">Add New Page</a>

    <!-- Responsive table wrapper -->
    <div class="table-responsive">
        <table class="table table-bordered table-striped align-middle mt-1">
            <thead class="table-dark">
                <tr>
                    <th>Page Title</th>
                    <th class="text-center">Container</th>
                    <th class="text-center">Page Content Children</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.Title)</td>
                        <td class="text-center">@Html.Raw(item.Container)</td>
                        <td class="text-center">
                            @foreach (var content in item.Contents)
                            {
                                @content.Title

                                <br />
                            }
                        </td>
                        <td class="text-center align-middle">
                            <div class="d-flex justify-content-center align-items-center flex-row gap-2 justify-content-end">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Details</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>
